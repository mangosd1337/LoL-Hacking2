// Generated by CoffeeScript 1.10.0
(function() {
  var APP_PORT, Mastery, Riot, Version, _mastery, _riot, _version, app, express;

  express = require('express');

  app = express();

  require('console-stamp')(console, {
    metadata: function() {
      return "[PID:" + process.pid + "]";
    },
    colors: {
      stamp: 'yellow',
      label: 'white',
      metadata: 'green'
    }
  });

  APP_PORT = 80;

  Mastery = require('./api/mastery');

  Version = require('./api/version');

  Riot = require('./api/riot');

  _mastery = new Mastery;

  _version = new Version;

  _riot = new Riot(_version);

  app.get('/masteries/*/*', function(request, response) {
    return _mastery.handle(request, response);
  });

  app.get('/deploy/core/*', function(request, response) {
    return _version.handle(request, response);
  });

  app.get('/riot/info*', function(request, response) {
    return _riot.handle(request, response);
  });

  app.listen(APP_PORT, function() {
    return console.info("EloBuddy.API launched on port '" + APP_PORT + "'");
  });

}).call(this);
